# AI-tutor
При запуску коду вводити команди в термінал:
pip install google-generativeai
pip install pygame
pip install gTTS
pip install pyperclip
pip install Pillow
pip install SpeechRecognition
pip install matplotlib
pip install numpy
pip install reportlab
pip install pyaudio
Якщо вилазить помилка звязана з аудіо:
pip install pipwin
pipwin install pyaudio
